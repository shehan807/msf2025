name: docs_msf2025
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  
  - pydata-sphinx-theme
  - sphinx-design
  - sphinx-copybutton

  - numpy 
  - matplotlib

    # Pip-only installs
  - pip:
    - -e ../

